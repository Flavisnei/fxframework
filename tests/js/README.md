Testes DOM do FxSelect2 (Node 18 ou superior):

```sh
cd tests/js
npm ci --ignore-scripts
npm test
```

Dependências apenas de teste; não fazem parte da aplicação PHP nem do Core. Os testes DOM não substituem inspeção visual num navegador.
